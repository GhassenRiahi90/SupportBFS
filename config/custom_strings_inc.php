<?php
/**
 * Support BFS — Chaînes de traduction / libellés visibles
 * (Phase 2 : branding ; Phase 3 : vocabulaire métier complet)
 */

if( $g_active_language === 'french' ) {
	$s_mantis_link = 'Portail Support BFS';
} else {
	$s_mantis_link = 'BFS Support Portal';
}
